apiVersion: v1
kind: ConfigMap
metadata:
  name: pod-config
  labels:
    purpose: config
    env: dev
data:
  COURSE: "DevOps with AWS"
  DURATION: "120HRS"
  TRAINER: "SIVAKUMAR REDDY M"

  # Commands

  # kubectl apply -f 08-configmap.yaml 

  # kubectl get configmaps

      # now u can see the configmap with the name pod-config and we have to attach this to a pod, so for that we have to write a pod definition file

  # 